# Build provenance

The signed release workflow creates GitHub build provenance attestations for the APK and AAB. These bind the produced binary digest to the GitHub Actions workflow that built it, complementing Android's application signature and the published SHA-256 checksums.
