<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose paints everything; this only covers the window before it does. -->
    <style name="Theme.Skald" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/skald_bg</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
